# AutoMaticWorker 产品与开发路线图

更新日期：2026-09-22
状态：M0–M2 已完成工程实现与本机自动化验收，当前版本 0.2.0。新增 M2.1 浏览器与双模式交互 SDK，M2.1 及 M3–M5 均为规划。已实现的接口和命令以 [流程规范](docs/FLOW_SPEC.md)、[制作说明](docs/AUTHORING.md) 为准；验证证据和限制见 [M2 验收报告](docs/M2_ACCEPTANCE.md)。

## 1. 产品定位

AutoMaticWorker 是以完全离线运行为核心的开源自动化平台与 SDK，面向内网和涉密环境的本地部署需求。数据、流程、配置、日志和结果留在部署环境内，安装、运行和升级不依赖公网。完整离线交付与目标环境验收仍需按阶段实现，见 [完全离线与内网部署要求](docs/OFFLINE_DESIGN.md)。

开发者掌握基础 Python、简单的 Playwright 页面操作，配合通用 SDK 即可快速制作标准流程包；使用者通过导入、配置和运行完成重复工作。SDK 保持贴近 Playwright 的页面与 Locator 模型，复用环境、会话、交互、日志和取消能力，降低重复开发成本。

商业模式：开源通用平台、自动化 SDK、流程包规范和 AI 制作指导 Skill；通过付费定制流程包、部署适配和后续维护获得收入。具体业务流程独立交付。

核心体验：

```text
业务需求 → 开发者 / AI + 制作 Skill → 标准流程包
                                      ↓
用户导入 → 填写参数 → 试运行 → 正式运行 → 查看结果
                                      ↓
                              日志与反馈 → 修复流程包
```

AI 是制作、检查和维护流程包的可选辅助，平台执行不依赖模型或外部 AI 服务。开发者可以手工制作；隔离环境可使用其允许的本地模型及本地 Skill。平台不引入云账号、在线激活、遥测或自动联网更新依赖。

## 2. 开工时的起点（保留用于对照）

基于仓库静态检查，尚未进行完整运行验证：

- 主分支包含 Vue + Flask + Pywebview 演示框架。
- 后端只有演示接口，前端流程按钮尚未连接执行功能。
- `process/demo_process` 已区分流程、步骤、元素和配置，但示例仅循环输出文字。
- 尚无完整的流程包规范、导入器、执行器、参数表单和执行记录。
- 启动仍依赖前端开发服务器，依赖声明及应用退出处理需要修正。
- `original_process` 分支和 `process` 标签包含具体业务脚本；它们不作为公开示例和平台内置流程。公开发布前需明确这些远程引用与历史内容的处理方案；本路线图不执行历史改写或远程删除。

## 3. 产品策略与边界

| 维度 | 决策或待验证假设 |
| --- | --- |
| 愿景 | 让内网和隔离环境中的重复工作，可以完全离线地制作、交付和自动执行 |
| 用户 | 内网与隔离部署单位的流程使用者；掌握基础 Python / Playwright 的开发者；在允许的本地环境借助 AI 制作流程的人员 |
| 成本 | 平台复用配置、运行、日志与打包能力；减少每个定制项目的重复开发。暂不承诺低价，先记录真实交付成本 |
| 价值 | 完全离线、数据本地留存；开发者用简单 Playwright + SDK 复用平台能力，聚焦业务流程；使用者通过界面完成配置与运行 |
| 取舍 | 首版聚焦 Windows、离线部署和单任务；可视化编排、多租户、本地调度与本地 AI 按需求排期，任何扩展都不能成为核心运行的公网依赖 |
| 指标 | 北极星指标为成功完成真实工作并被客户验收的流程运行次数；首个阶段重点指标为新作者从需求到首个可导入、可运行流程包的用时 |
| 增长 | 用开源示例、开发文档和 Skill 帮助发现与试用；通过需求沟通承接付费制作，记录询盘、试用和付费转化 |
| 能力 | 优先建立流程契约、运行器、配置表单、模板、校验和交付能力；复用现有 Python 与前端生态，避免过早重写技术栈 |
| 持续优势 | 来自业务理解、稳定交付经验、可复用适配和维护效率；开源规范或 Skill 本身不构成独占壁垒 |

成功运行应同时满足退出状态正常、输出符合声明、业务验收条件成立。开发测试和无实际价值的重复运行不计入北极星指标。指标基于本地记录和用户自行导出的非敏感反馈，不内置遥测或数据上传。

## 4. 阶段路线图

按验收门槛推进，不按固定日历强行发布。当前未确定人员投入和首个客户需求，暂不承诺工期。

| 阶段 | 目标 | 关键交付物 | 进入下一阶段的条件 |
| --- | --- | --- | --- |
| M0：工程基线 · 已完成 | 可重复启动并确定公开范围 | 启动说明、依赖修正、目录边界、示例选择 | 新建虚拟环境已安装验证；真实桌面关闭可取消任务并停止服务 |
| M1：规范与 AI 制作基础 · 已完成 | 确立可运行的最小契约 | 规范、Schema、最小 SDK / 执行器、模板、校验器、Skill Alpha | 两个公开示例通过；独立 AI 制作的流程包完成 9 项行为验证 |
| M2：桌面使用闭环，v0.2 · 已完成工程验收 | 无需修改代码完成导入和运行 | 流程库、参数表单、运行页、日志和结果页 | 浏览器实际验证 ZIP 导入、文件选择、配置保存、试运行、正式运行、取消及结果下载；真实非技术用户试用待后续开展 |
| M2.1：浏览器与交互 SDK · 规划 | 开源复用浏览器、点击与输入能力 | Playwright 标准后端、Patchright 可选后端、轨迹与逐字输入、Windows 输入模式、环境管理 | 两后端本地页面验证；系统输入焦点/DPI/取消验证；流程包导入闭环；无重复提交及资源泄漏 |
| M3：AI 制作质量，v0.3 | AI 生成与修复可重复验证 | Skill v1、制作辅助命令、评测用例、兼容性检查 | 全新会话只依赖需求和公开材料即可完成代表性流程包 |
| M4：可交付试用版，v0.4 | 完成离线客户交付 | Windows 完整离线安装包、运行组件、离线文档、更新与回滚材料 | 无公网且无缓存的干净环境完成安装/首次运行/升级；外联观测通过；至少一个明确验收的隔离环境试点完成闭环 |
| M5：稳定版，v1.0 | 根据使用反馈稳定契约 | 兼容策略、升级与迁移、维护流程 | 核心契约稳定，试点暴露的阻塞问题解决，升级不破坏已有合格流程包 |

依赖顺序：M0 → M1 → M2 → M2.1 → M3 → M4 → M5。Skill 在 M1 即参与制作验证，M2 的使用反馈反向修正规范；M2.1 先使浏览器接口可执行，M3 再冻结对应 AI 制作说明。界面草图和文档可提前推进，但不能先于运行契约承诺功能。

### M0：工程基线

- 修正前后端依赖归属、缺失依赖及 Axios 初始化顺序。
- 明确开发启动与发布启动方式，发布模式加载构建产物。
- 管理后端与任务进程的启动、就绪检查和退出。
- 清理或隔离模板演示页面，保留可用的基本应用框架。
- 记录当前许可证与公开范围，单独确定商业流程包的交付条款；不在本阶段擅自修改许可或远程历史。

验收：在记录的环境版本下，从安装依赖到打开窗口有可复现步骤；关闭窗口不会遗留本应用启动的服务进程。

### M1：流程包规范与制作基础

先让命令行运行器执行标准流程包，再让桌面界面接入同一套能力。

规范至少覆盖以下契约：

| 契约 | 必须明确的内容 |
| --- | --- |
| 元数据 | 包 ID、名称、说明、作者、版本、规范版本、平台兼容范围 |
| 入口 | 如何定位入口、传入配置、获取结果；不把宿主内部模块当作公共接口 |
| 参数 | 文本、数字、布尔、枚举、文件和目录；必填、默认值、约束及敏感字段标识 |
| 运行上下文 | 本次运行 ID、输出目录、日志、进度、取消信号和运行模式 |
| 结果 | 成功 / 失败 / 取消状态、摘要、输出文件和可定位的错误信息 |
| 依赖 | 依赖声明、安装时机、失败提示和与其他流程包的隔离方式 |
| 外部能力 | 网络访问、文件读写、浏览器操作和外部系统写入的声明 |
| 生命周期 | 导入、校验、配置、启动、取消、结束与升级行为 |
| 兼容性 | 如何拒绝不支持的规范或平台版本，如何保留旧包兼容性 |

权限声明用于告知与检查；声明、独立进程和依赖隔离均不等于已经实现安全沙箱，不对任意来源的 Python 包作安全执行承诺。

包格式已确定为 ZIP 与 JSON 清单，正式字段见规范。以下目录中的 steps、assets 和 tests 按流程需要选择：

```text
example-flow/
├── manifest.json        元数据、参数、入口、依赖和能力声明
├── flow.py              执行入口
├── steps/               按需拆分的步骤
├── assets/              执行所需的非敏感资源
├── tests/               样例输入及预期输出
└── README.md            使用说明和限制
```

配套交付：

- 机器可读 Schema、开发文档和可运行参考模板。
- 初始化、校验、试运行、打包的最小工具；命令名称在实现时统一。
- 模板只调用已实现的公开接口。
- 校验包含清单、入口、兼容性、归档路径和资源限制；导入检查不运行包内任意业务代码。
- 第一份公开示例使用合成数据生成本地报告；第二份示例操作本地模拟网页。
- 配套 Skill Alpha 随规范版本更新。

验收：两个示例都能通过校验，完成执行并产生预期结果；故意缺字段或版本不匹配的包能得到明确错误。

### M2：桌面使用闭环

- 流程库：导入本地包、查看说明与版本、卸载和更新。
- 配置页：根据参数声明生成表单；校验输入，保存非敏感配置。
- 运行页：展示任务状态、步骤进度、日志、取消操作和最终结果。
- 历史页：按运行记录定位输入配置摘要、错误和输出文件。
- 单任务优先；为每次运行建立独立工作目录和任务进程。
- 敏感参数不写入普通配置导出和日志；首版优先每次输入，需要持久化时再提供明确的保存方式。
- 明确失败重试的边界；涉及外部写入的步骤不做无条件自动重试。

试运行区分两种能力：本地模拟执行与外部操作预览。流程包应声明支持程度；平台不能仅凭一个开关保证第三方代码不会产生副作用。不支持试运行时明确显示限制。

验收：没有开发环境操作经验的试用者可以按使用说明完成一次任务；失败、取消和重复启动有明确结果；日志不包含示例敏感值。

### M2.1：浏览器与双模式交互 SDK

选型依据、接口设计和具体测试矩阵见 [浏览器 SDK 调研](docs/BROWSER_SDK_RESEARCH.md)。本阶段尚未实现；v0.2 的 `browser` 声明不代表已有浏览器运行环境。

- 标准后端采用 Playwright；Patchright 作为可选 Chromium 后端，按相同用例验证，不承诺无法检测。
- 同时支持浏览器内模拟与 Windows 系统输入，默认浏览器内模拟；模式显式选择，不自动切换占用用户鼠标。
- 将随机曲线、悬停、按下/释放、逐字输入封装为通用动作，补齐目标校验、焦点检查、结果确认和取消。
- 浏览器环境与用户数据目录由平台管理；明确临时会话、持久化登录及外部接管的生命周期。
- 浏览器、驱动运行资源及依赖使用已准备的本地版本；环境预检和运行不触发公网下载，缺失组件只报告本地诊断。
- 系统输入具备桌面互斥、DPI/坐标处理、失焦停止与按键释放；中文文本提交和真实输入法过程分别定义。
- 包规范、执行器、CLI、前端和示例同步升级，加入可取消的人工登录/验证衔接，保留旧标准库包兼容性。
- 私有业务定位器、站点及数据保留在独立流程包。公共示例只操作本地模拟页面。

实施顺序：A 环境与会话 → B 浏览器公共动作及 Patchright 适配 → C Windows 系统输入 → D 平台与流程包闭环。实际接口通过测试后，再更新制作 Skill。

验收：本地测试页证明正确命中、输入值正确、外部写入不盲目重试、取消可停止、资源可清理；双模式和 DPI 用例分别验证。反检测效果须另行受控评估，不能由本地演示通过推断。

### M3：AI 制作 Skill 与质量验证

目标是让 AI 能制作、修改和修复标准流程包，并用工具结果证明交付状态。

已建立项目内路径：`skills/automaticworker-flow-author/SKILL.md`，当前为与规范 1.0 匹配的 Alpha。M3 将继续扩大场景评测并形成 Skill v1。

作者材料、模板与工具全部本地可用；评测同时覆盖手工制作及可选本地 AI 辅助。平台运行不依赖在线模型，设计 API 必须在实际实现后才纳入 Skill。

Skill 负责以下工作：

1. 理解目标、输入、输出、运行环境和验收要求，只补问阻塞制作的信息。
2. 检查目标平台和规范版本，读取对应的正式契约与模板。
3. 将需求拆成可验证步骤，区分读取、计算和外部写入。
4. 使用模板生成清单、入口实现、配置说明和测试样例。
5. 使用合成数据、本地模拟页面或明确授权的测试环境验证。
6. 运行确定性校验和行为测试，根据实际报错修复。
7. 输出流程包、使用说明、兼容版本、测试证据与未验证项。

Skill 的核心约束：

- 不猜测不存在的 SDK、接口、命令和参数字段。
- 明确区分生成代码、模拟验证、真实系统执行与发布授权。
- 不把账号、会话凭据、客户数据或测试输出装入交付包。
- 对外部写入和重试，遵循当前用户授权及流程定义的停止条件。
- 生成后未执行的内容明确标为未验证，不能把 Schema 通过等同于业务可用。
- 修改既有流程时保留客户需求与必要兼容性，说明迁移影响。

资源组织：

| 资源 | 内容与维护方式 |
| --- | --- |
| `SKILL.md` | 触发范围、制作步骤、验证要求和引用入口，保持简洁 |
| 正式规范与 SDK 文档 | 接口唯一来源，Skill 按需引用，不维护另一份重复契约 |
| 制作模板 | 与当前规范匹配的可运行示例；随平台一起测试 |
| 校验 / 打包工具 | 可重复执行的确定性检查，不依赖 AI 自行判断包是否合格 |
| 评测案例 | 输入需求、测试资源、验收条件与实际执行结果 |

先以仓库随附 Skill 交付，随后支持带对应规范、模板与工具的独立分发包。独立分发时检查所有引用可解析，不依赖作者电脑的绝对路径。先验证一种实际使用环境，其他 AI 工具的安装与加载方式逐个验证后再声明支持。

第一轮评测至少包含：

- 合成 CSV 数据清洗并生成本地报告。
- 文件分类的预览与执行，在专用临时目录测试。
- 本地模拟网页的填表与结果提取。
- 必填参数缺失、输入格式错误和取消运行。
- 在模拟服务中验证超时、失败和避免重复写入。
- 对已有流程包新增参数或修复错误，检查兼容性。

验收：三个制作场景分别在全新会话中从需求开始，产出通过校验和预期输出检查的流程包；记录是否一次通过、修复轮数、人工修改点和耗时。首轮建立基线，再决定可接受阈值，不预先宣传成功率。

### M4：试用发布与商业交付

- 提供完整 Windows 离线安装包，包含经验证的运行时、浏览器与 WebView2 离线材料及构建后的前端，目标环境无需联网或安装开发工具。
- 在无公网且无包缓存的干净环境验证安装、首次启动、导入、执行、取消、结果导出、本地升级及回滚；观测平台和子进程的外联尝试，依据 [离线验收矩阵](docs/OFFLINE_DESIGN.md) 留存证据。
- 首页和 README 清楚说明定位、当前能力、安装步骤及公开示例。
- 提供使用者快速入门、流程作者文档、AI Skill 使用案例。
- 每个商业项目形成需求范围、输入输出样例、验收条件、报价和维护边界。
- 交付包附兼容版本、配置说明、演示材料与支持方式。
- 客户专用流程与资料保存在独立的交付空间。
- 通过一个范围可控的内网或隔离环境客户试点测量配置成本、交付工时、故障定位成本和维护投入。

验收：客户可完全离线安装、导入、运行和升级，结果符合约定；交付与维护范围可追溯。涉密场景按使用单位的准入与部署要求验收，不把一般离线功能测试表述为资质认证。

### M5：稳定化与后续选择

根据试点数据决定优先级：

- 经常需要多人复用：完善流程包分发与版本更新。
- 经常需要无人值守：评估定时任务、恢复机制和运行环境管理。
- 经常需要多个任务：增加队列、并发控制和资源限制。
- 经常由业务人员自行制作：优化 Skill、需求模板与诊断工具。
- 确有跨平台需求：再扩大桌面系统支持范围。

v1.0 的重点是已有能力稳定、包升级可控和交付可重复。新增功能不能替代这些发布条件。

## 5. 验证商业与技术假设

| 假设 | 最低成本验证方式 | 记录的证据 |
| --- | --- | --- |
| 用户愿意使用统一平台运行流程 | 让目标用户安装平台并运行公开示例 | 是否独立完成、卡点和耗时 |
| 固定规范能降低 AI 制作成本 | 同一组任务比较自由制作与使用 Skill 制作 | 人工修改点、总耗时、可运行率 |
| 流程包能减少定制交付的重复开发 | 连续完成两个不同需求的流程包 | 平台复用比例、额外平台改动和交付工时 |
| 客户愿意为定制与维护付费 | 提供范围清楚的试点报价 | 接受原因、拒绝原因和实际维护投入 |

报价模型先按需求复杂度、系统适配、部署和维护工作量估算。获得真实工时后再制定标准服务档位。

## 6. 第一轮可执行工作清单

按依赖排序：

- [x] 修正启动链路、依赖声明与退出行为，记录可复现开发步骤。
- [x] 明确公开仓库内容范围及历史业务代码的处理方案（不执行远程历史改写）。
- [x] 写出流程包规范与可校验清单。
- [x] 实现最小运行上下文、日志、结果和取消接口。
- [x] 制作合成数据报告及本地网页流程包与对应测试。
- [x] 提供初始化、校验、执行和打包的基本工具。
- [x] 编写与真实接口匹配的 Skill Alpha，并完成独立制作验证。
- [x] 接入桌面导入、自动表单、执行与结果展示。

M0–M2 的工程交付已完成；M2.1 的浏览器与双模式交互 SDK、M3 的多场景评测、M4 的免开发环境安装包与客户试用、M5 的稳定兼容承诺尚未交付。
